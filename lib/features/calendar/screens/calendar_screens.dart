import 'package:flutter/material.dart';

class CalendarScreens extends StatelessWidget {
  const CalendarScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Calendar")));
  }
}
